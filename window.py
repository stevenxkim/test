from tkinter import *

# Create a window
root = Tk()
root.title("My banking App interface")

#Run on the main window loop
root.mainloop ()

#Create a window
root = Tk()
root.title("My GUI APP")
#Create a label and add it to the window using pack()
label1 = Label(root, text="MY GUI APP")
label1.pack()
#Create a second label with longer text and add it to the window using pack()
label2 = Label(root, text="MY GUI APP!")
label1.pack()
# Create a second label with longer text and add it to the window using pack()
label2 = Label(root, text="lable2 with longer sentence!")
label2.pack()
# Run the main window loop
root.mainloop()










